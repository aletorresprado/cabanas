//Para manejar las rutas relacionadas con los usuarios, como el perfil, la configuración, etc.
const express = require('express');

const router = express.Router();
// Llego con la ruta base: /api/v1/users

// Endpoint para registrar un nuevo usuario





module.exports = router;
// Manejar las rutas relacionadas con los favoritos, agregar o quitar cabañas de la lista de favoritos del usuario
const express = require('express');

const router = express.Router();
// Llego con la ruta base: /api/v1/favorites

// Endpoint para registrar un nuevo usuario





module.exports = router;